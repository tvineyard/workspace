# workspace

A general-purpose space for small projects, experiments, and testing. Nothing formal lives here yet — it's a catch-all for whatever I end up needing GitHub for.

## Layout

Two folders to start: `projects/` holds self-contained small projects (one folder each), and `sandbox/` is for throwaway experiments and quick tests. There's no need to stick to this — feel free to just drop things in.
